# Assets

Drop your files here:

- `profile.jpg` - your photo, shown on the home page (falls back to an initial avatar if missing)
- `resume.pdf` - your resume, enables the "Download Resume" button (hidden if missing)
- Any project screenshots, e.g. `project1.png`, referenced from `config.py`
